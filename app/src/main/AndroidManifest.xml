<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tirth.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".PersonalInformation" />
        <activity android:name=".EducationDetails" />
        <activity android:name=".SelectProfession" />
        <activity android:name=".SkillActivity" />
        <activity android:name=".IntrestActivity" />
        <activity android:name=".Experience" />
        <activity android:name=".Project" />
        <activity android:name=".Reference" />
        <activity android:name=".FinalActivityDisplay"></activity>

    </application>

</manifest>